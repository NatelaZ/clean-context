---
name: clean-context
description: Аудит и уборка стартовой платы за контекст Claude Code. Использовать при запросах «почисти контекст», «что жрёт контекст», «clean-context», «какие скилы/MCP/агенты лишние». Делает полный обход конфигурации, сверяет с реальной статистикой использования и обратимо отключает неиспользуемое.
---

# Клинер контекста

Скилл аудита и обратимой уборки того, что грузится в контекст на старте сессии:
скилы, агенты, MCP-серверы, плагины, CLAUDE.md, hooks.

## Когда использовать
Запросы вида «почисти контекст», «что жрёт контекст», «покажи лишнее», «clean-context».

## Как действовать

Скрипты лежат в каталоге ЭТОГО скилла. Его абсолютный путь сообщён тебе при
запуске как «Base directory for this skill» — ниже он обозначен `SKILLDIR`.
Подставляй вместо `SKILLDIR` фактический путь (он работает и при установке как
скилл в `~/.claude/skills`, и при установке как плагин из маркетплейса).

1. **Аудит.** Запусти:
   `node "SKILLDIR/bin/audit.js"`
   Покажи пользователю отчёт целиком. Он только читает, ничего не меняет.

   **Точность оценки (по желанию).** Цифры аудита — это оценка и могут расходиться
   с реальным `/context` примерно на 20–25%. Чтобы откалибровать: открой `/context`,
   посмотри реальные токены категории, затем:
   `node "SKILLDIR/bin/calibrate.js" agent 2100`
   Поправка сохранится в `.state/calibration.json` и применится к будущим аудитам;
   индикатор калибровки виден в отчёте.

   Соответствие категорий `/context` → аудита (важно, иначе двойной счёт):
   - `Custom agents` → `agent` (чистая пара).
   - `Skills` в `/context` объединяет И личные скилы, И скилы плагинов, а аудит делит
     их на `skill` + `plugin`. Поэтому калибруй `skill` числом = `Skills` − вес
     `plugin` из аудита (если плагинов нет — просто `Skills`).
   - `claudemd`/`mcp`/`hook` в `/context` отдельно не выделены так же, как в аудите
     (например, `claudemd` аудита — это все CLAUDE.md проектов, а `/context` показывает
     лишь текущую сессию) — их по `/context` не калибруют, оставь оценку как есть.

2. **Обсуждение.** Из секции «Рекомендую отключить» предложи пользователю, что
   отключить. Пометка `[авто]` — низкий риск (скил/MCP, обратимо), `[спрос]` —
   спрашивай отдельно. Агенты из «решить вручную» — только по явному выбору
   пользователя. Никогда не отключай ничего без подтверждения.

3. **Применение.** На выбранные имена запусти:
   `node "SKILLDIR/bin/apply.js" <имя1> <имя2> ...`
   Имена берутся из последнего аудита. Операции обратимы (перенос, не удаление),
   пишутся в лог.

4. **Откат.** Чтобы вернуть всё, что отключено с момента последнего отката (журнал кумулятивный):
   `node "SKILLDIR/bin/restore.js"`

5. **Напомни:** изменения вступают в силу с новой сессии Claude Code (стартовая
   плата фиксируется при запуске).

6. **Разбор CLAUDE.md (по запросу или когда `claudemd` доминирует в аудите).** Если
   в отчёте категория `claudemd` — крупнейшая, или пользователь просит «разбери
   CLAUDE.md», запусти:
   `node "SKILLDIR/bin/claudemd.js"`
   Покажи разбор и помоги решить, что вынести/сжать: какие секции общие для
   нескольких проектов (→ в глобальный `~/.claude/CLAUDE.md`), какие раздуты, где
   «вода». Правки вносит только пользователь — инструмент прозу CLAUDE.md не редактирует.

7. **Тренды (по запросу «тренды» / «как менялась стартовая плата»).** Запусти:
   `node "SKILLDIR/bin/trends.js"`
   Покажи динамику стартовой платы по снимкам и что менялось (добавилось/убыло/выросло).
   Снимки копятся сами (раз в день при аудите/старте сессии).

## Важно
- Только перемещение в `*-disabled`, никогда не удаление.
- Перед правкой `settings.json`/`.claude.json` создаётся `.bak`.
- Служебные файлы Claude Code (`.last-cleanup` и т.п.) не трогаются.
